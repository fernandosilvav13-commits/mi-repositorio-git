# Plan 12-03: Integration into CVProcessor + Full Tests

**Phase:** 12 - Post-Processing Rules Expansion
**Plan:** 03
**Wave:** 2
**Objective:** Wire RuleRegistry into CVProcessor._post_process and add integration tests.
**Gap closure:** false

## Tasks

### Task 1: Integrate rules into CVProcessor._post_process

Modify `backend/app/services/cv_processor.py`:

- Import rule_registry and evaluate_rules
- After existing post-processing (gender, phone, RUT), call `evaluate_rules(text, data)`
- Only apply results from ENABLED rules (shadow rules log but don't write)
- Log shadow rule output for monitoring

### Task 2: Enable rules via config

Simple enabled_rules list in RuleRegistry:

```python
rule_registry.enable("nationality")
rule_registry.enable("date_of_birth")
```

### Task 3: Integration tests

File: `backend/tests/test_rules_integration.py`

- Test CVProcessor post_process with rules enabled
- Test CV text with nationality indicator → NACIONALIDAD field populated
- Test CV text with birth date → FECHA_NACIMIENTO field populated
- Test rules do NOT override existing LLM values (shadow mode principle)
- Test all rules run in shadow without crashing

## Verification

- Rules integrate with CVProcessor
- Enabled rules write fields; disabled rules log only
- Existing post-processing (gender, phone, RUT) still works
- No regressions in test_post_processing.py tests
