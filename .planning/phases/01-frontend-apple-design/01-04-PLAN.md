---
phase: 01-frontend-apple-design
plan: 04
type: execute
wave: 4
depends_on: ["01-02"]
files_modified: [
  "frontend/src/app/wizard/page.tsx",
  "frontend/src/components/apple/ConfiguratorCard.tsx"
]
autonomous: true
requirements: [01-WIZARD]

must_haves:
  truths:
    - "Wizard flow feels like an Apple 'Configure' or 'Shop' experience"
    - "Options are presented as pill chips or white utility cards (18px radius)"
  artifacts:
    - path: "frontend/src/app/wizard/page.tsx"
      provides: "Redesigned Product Configurator Wizard"
  key_links:
    - from: "frontend/src/app/wizard/page.tsx"
      to: "frontend/src/app/globals.css"
      via: "utility classes (rounded-pill, rounded-lg)"
---

<objective>
Overhaul the Wizard flow as a "Product Configurator" following Apple's e-commerce design patterns.

Purpose: Makes the CV ingestion and extraction setup feel like a high-end configuration process.
Output: Redesigned multi-step wizard using Apple utility cards and pill-shaped controls.
</objective>

<execution_context>
@/home/fernandosilvav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/fernandosilvav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-frontend-apple-design/01-CONTEXT.md
@.planning/phases/01-frontend-apple-design/01-RESEARCH.md
@.planning/phases/01-frontend-apple-design/01-PATTERNS.md
@frontend/src/app/wizard/page.tsx (Original analog)
@apple/DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Wizard as a Product Configurator</name>
  <files>frontend/src/app/wizard/page.tsx</files>
  <read_first>
    - frontend/src/app/wizard/page.tsx
    - .planning/phases/01-frontend-apple-design/01-UI-SPEC.md
  </read_first>
  <action>
    Redesign the wizard/page.tsx layout.
    
    1. Base layout: Use a Parchment (#f5f5f7) background for the entire configurator area.
    2. Header: Centered headline (display-md: 34px/600) with step indicator (caption-strong: 14px).
    3. Content: A centered 1-column stack of ConfiguratorCards.
    4. Nav: A persistent "Buy/Continue" primary pill button on the right of the SubNav (already established in Plan 01).
  </action>
  <verify>
    Check wizard/page.tsx for parchment background and display-md headlines.
  </verify>
  <done>Wizard chassis is transformed into a Product Configurator.</done>
  <acceptance_criteria>
    - Page background is Parchment (#f5f5f7).
    - Headlines use SF Pro Display weight 600.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Utility Cards and Pill Chips</name>
  <files>frontend/src/components/apple/ConfiguratorCard.tsx, frontend/src/app/wizard/page.tsx</files>
  <read_first>
    - apple/DESIGN.md
    - .planning/phases/01-frontend-apple-design/01-RESEARCH.md
  </read_first>
  <action>
    1. Create ConfiguratorCard.tsx:
       - Background: Pure White (#ffffff).
       - Radius: 18px (rounded-lg).
       - Border: 1px hairline (#e0e0e0).
       - Padding: 24px.
    2. Implement Pill Chips for selections:
       - Rounded: full pill (9999px).
       - Active state: Action Blue border and text.
    3. Replace standard inputs/buttons in the Wizard with these components.
  </action>
  <verify>
    Check ConfiguratorCard.tsx for rounded-lg and pill chips for rounded-full.
  </verify>
  <done>Configuration options use Apple-style white cards and pill controls.</done>
  <acceptance_criteria>
    - Options are contained in white cards with 18px radius.
    - Selection chips use the pill shape.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Integrate Existing State Logic to New UI</name>
  <files>frontend/src/app/wizard/page.tsx</files>
  <read_first>
    - frontend/src/app/wizard/page.tsx (Original state logic)
  </read_first>
  <action>
    Ensure all existing wizard state logic (file uploads, template selection, crossref config) works with the new UI components.
    
    1. Connect the ConfiguratorCard selections to the `currentStep` and `subStep` state.
    2. Ensure the "Next" button in the SubNav correctly advances the state.
    3. Verify that validation errors (Zod) are displayed using SF Pro Text in red (#ff3b30 or similar Apple error red).
  </action>
  <verify>
    Manually verify (or simulate) step transitions and state updates in the redesigned page.
  </verify>
  <done>Configurator functionality matches the original Wizard logic.</done>
  <acceptance_criteria>
    - Step transitions work correctly.
    - Selected options are persistent across steps.
    - Forms submit to the backend as before.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Configurator→API | Input data (RUTs, file paths) sent to backend; must be validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Injection | Wizard Forms | mitigate | Zod schema validation on all inputs before submission. |
</threat_model>

<verification>
- E2E Flow: Complete a full ingestion-to-extraction setup in the new Configurator.
- Interaction Check: Verify active scale triggers on cards and buttons.
</verification>

<success_criteria>
- Wizard UI uses White Utility Cards (18px radius) and Pill Chips.
- Configurator flow successfully passes data to the existing backend endpoints.
- Page has no "SaaS" elements (borders, gray backgrounds, standard buttons).
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-apple-design/01-04-SUMMARY.md`
</output>
