---
phase: 01-frontend-apple-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "frontend/src/app/globals.css",
  "frontend/src/app/layout.tsx",
  "frontend/src/components/layout/GlobalNav.tsx",
  "frontend/src/components/layout/SubNav.tsx"
]
autonomous: true
requirements: [01-COLORS, 01-TYPE, 01-NAV]

must_haves:
  truths:
    - "Typography uses negative tracking (-0.011em) for headlines >17px"
    - "Color palette includes Action Blue (#0066cc), Parchment (#f5f5f7), and Near-Black (#272729)"
    - "Navigation is dual-layered: 44px Black Nav + 52px Frosted Sub-Nav"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Tailwind v4 @theme configuration"
    - path: "frontend/src/components/layout/GlobalNav.tsx"
      provides: "Sticky 44px black header"
    - path: "frontend/src/components/layout/SubNav.tsx"
      provides: "Sticky 52px frosted glass sub-nav"
  key_links:
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/components/layout/GlobalNav.tsx"
      via: "import and render"
    - from: "frontend/src/app/layout.tsx"
      to: "frontend/src/components/layout/SubNav.tsx"
      via: "import and render"
---

<objective>
Establish the foundational Apple design system and the dual-navigation shell.

Purpose: Locks in the core visual identity (colors, typography) and layout structure that all subsequent features will use.
Output: Configured Tailwind v4 theme and a persistent, sticky navigation system.
</objective>

<execution_context>
@/home/fernandosilvav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/fernandosilvav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-frontend-apple-design/01-CONTEXT.md
@.planning/phases/01-frontend-apple-design/01-RESEARCH.md
@.planning/phases/01-frontend-apple-design/01-UI-SPEC.md
@apple/DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Tailwind v4 Apple Theme</name>
  <files>frontend/src/app/globals.css</files>
  <read_first>
    - frontend/src/app/globals.css
    - apple/DESIGN.md
    - .planning/phases/01-frontend-apple-design/01-RESEARCH.md
  </read_first>
  <action>
    Overhaul the @theme block in globals.css to match the Apple Design Specification.
    
    1. Define colors:
       --color-action-blue: #0066cc;
       --color-parchment: #f5f5f7;
       --color-near-black: #272729;
       --color-ink: #1d1d1f;
    2. Define typography:
       --font-display: "SF Pro Display", "Inter", system-ui;
       --tracking-apple-tight: -0.011em;
    3. Define radii:
       --radius-sm: 8px;
       --radius-lg: 18px;
       --radius-pill: 9999px;
    4. Define the signature shadow:
       --shadow-product: rgba(0, 0, 0, 0.22) 3px 5px 30px;
    
    Remove all previous "luxury" or non-Apple tokens.
  </action>
  <verify>
    grep -E "color-action-blue|tracking-apple-tight|radius-lg" frontend/src/app/globals.css
  </verify>
  <done>Tailwind v4 theme reflects Apple design tokens exactly.</done>
  <acceptance_criteria>
    - Action Blue is #0066cc.
    - Tracking Apple Tight is -0.011em.
    - Radius LG is 18px.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Implement Global and Frosted Sub-Nav</name>
  <files>frontend/src/components/layout/GlobalNav.tsx, frontend/src/components/layout/SubNav.tsx, frontend/src/app/layout.tsx</files>
  <read_first>
    - frontend/src/app/layout.tsx
    - .planning/phases/01-frontend-apple-design/01-UI-SPEC.md
    - apple/DESIGN.md
  </read_first>
  <action>
    1. Create GlobalNav.tsx:
       - Height: 44px
       - Background: black
       - Sticky: top-0, z-50
       - Content: Minimalist nav links (12px, white).
    2. Create SubNav.tsx:
       - Height: 52px
       - Background: var(--color-parchment) at 80% opacity
       - Backdrop-blur: md (12px)
       - Sticky: top-[44px], z-40
       - Border-bottom: 1px hairline (#e0e0e0)
       - Content: Tagline (21px/600) on left, Primary Action (Pill button) on right.
    3. Integrate both into frontend/src/app/layout.tsx within the body shell.
  </action>
  <verify>
    Check layout.tsx for GlobalNav and SubNav imports and usage.
  </verify>
  <done>Persistent dual-navigation shell is implemented and sticky.</done>
  <acceptance_criteria>
    - GlobalNav height is exactly 44px.
    - SubNav uses backdrop-blur and is sticky below GlobalNav.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement Shared Interactive Behaviors</name>
  <files>frontend/src/app/globals.css, frontend/src/components/ui/button.tsx</files>
  <read_first>
    - frontend/src/app/globals.css
    - frontend/src/components/ui/button.tsx (if exists, or base button)
  </read_first>
  <action>
    1. Add the "Apple tight" utility class to globals.css:
       .apple-tight { font-family: var(--font-display); font-weight: 600; letter-spacing: var(--tracking-apple-tight); }
    2. Add the active scale transform to globals.css or button component:
       .active-scale:active { transform: scale(0.95); transition: transform 0.1s ease-out; }
    3. Ensure primary buttons use rounded-pill (radius: 9999px).
  </action>
  <verify>
    Check globals.css for .active-scale and button variants for rounded-pill.
  </verify>
  <done>Buttons and interactive elements feel "Apple" (scale on click, pill shapes).</done>
  <acceptance_criteria>
    - Primary buttons use rounded-pill class.
    - Active state triggers transform: scale(0.95).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→UI | Nav links and theme application are client-side only; no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | globals.css | accept | Theme colors and tokens are public by design. |
</threat_model>

<verification>
- Viewport scroll: Navbars remain sticky and properly layered.
- Typography check: Inspect headlines in DevTools to confirm negative letter-spacing.
</verification>

<success_criteria>
- Root layout renders the dual-nav system.
- All interactive elements use Action Blue (#0066cc).
- Scale-on-active behavior works on nav buttons.
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-apple-design/01-01-SUMMARY.md`
</output>
