---
phase: 01-frontend-apple-design
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: [
  "frontend/src/app/(gallery)/page.tsx",
  "frontend/src/app/extraction/page.tsx"
]
autonomous: true
requirements: [01-GALLERY]

must_haves:
  truths:
    - "Extraction results are displayed in a Tile-based Museum Gallery layout"
    - "Traditional tables are replaced by photographic-first artifact cards"
  artifacts:
    - path: "frontend/src/app/(gallery)/page.tsx"
      provides: "New Museum Gallery page"
    - path: "frontend/src/components/apple/ProductCard.tsx"
      provides: "Visual mapping of CV data to artifacts"
  key_links:
    - from: "frontend/src/app/(gallery)/page.tsx"
      to: "frontend/src/components/apple/Tile.tsx"
      via: "page composition"
---

<objective>
Redesign the extraction results view as a high-end "Museum Gallery" using the established Apple components.

Purpose: Transforms dry data tables into an aspirational product gallery.
Output: New gallery page layout and data-to-card mapping.
</objective>

<execution_context>
@/home/fernandosilvav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/fernandosilvav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-frontend-apple-design/01-CONTEXT.md
@.planning/phases/01-frontend-apple-design/01-RESEARCH.md
@.planning/phases/01-frontend-apple-design/01-PATTERNS.md
@frontend/src/app/extraction/page.tsx (Original analog)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Gallery Page Structure</name>
  <files>frontend/src/app/(gallery)/page.tsx</files>
  <read_first>
    - frontend/src/app/extraction/page.tsx
    - frontend/src/components/apple/Tile.tsx
    - frontend/src/components/apple/ProductCard.tsx
  </read_first>
  <action>
    Create the new gallery page at app/(gallery)/page.tsx.
    
    1. Compose the page using alternating Tile components:
       - White Hero Tile (Title: "Collection", Subtitle: "Extracted Insights").
       - Dark Product Tile (Grid of result artifacts).
       - Parchment Utility Tile (Export actions).
    2. Import the data fetching logic from the original extraction/page.tsx.
  </action>
  <verify>
    Check app/(gallery)/page.tsx for Tile usage and data fetching.
  </verify>
  <done>Gallery structure is established with Apple design rhythm.</done>
  <acceptance_criteria>
    - Page uses Tile components for sections.
    - Data is fetched from the existing API service.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Map Extraction Data to Product Artifacts</name>
  <files>frontend/src/app/(gallery)/page.tsx, frontend/src/components/apple/ProductCard.tsx</files>
  <read_first>
    - .planning/phases/01-frontend-apple-design/01-PATTERNS.md
    - apple/DESIGN.md
  </read_first>
  <action>
    Transform the table rows into ProductCard instances.
    
    1. For each extraction result, render a ProductCard.
    2. Map fields:
       - Filename -> Artifact Name.
       - Extraction score -> "Condition" or "Match".
       - RUT -> "Serial No."
    3. Use placeholder photography or generated SVG icons for the CV "artifact" imagery.
  </action>
  <verify>
    Check (gallery)/page.tsx for .map() function returning ProductCard components.
  </verify>
  <done>CV data is presented as museum artifacts in the gallery.</done>
  <acceptance_criteria>
    - Data fields from extraction results appear on ProductCards.
    - Visual representation avoids standard table cells.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Visual Polish and Grid Layout</name>
  <files>frontend/src/app/(gallery)/page.tsx</files>
  <read_first>
    - apple/DESIGN.md
  </read_first>
  <action>
    Refine the gallery grid:
    1. Use a 2-column or 3-column grid (depending on viewport).
    2. Vertical spacing between cards: 24px (spacing.lg).
    3. Section vertical padding: 80px (spacing.section).
    4. Ensure the Parchment footer/utility section is cleanly separated.
  </action>
  <verify>
    Check CSS classes for grid-cols-1, md:grid-cols-2, etc.
  </verify>
  <done>Gallery view is responsive and maintains high-end aesthetic.</done>
  <acceptance_criteria>
    - Grid is responsive (stacks on mobile).
    - Spacing matches DESIGN.md tokens.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API→Gallery | Extracted data is rendered; must be sanitized by React. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Gallery View | mitigate | React 19 automatic escaping for dynamic text rendering. |
</threat_model>

<verification>
- Data Integrity: Compare Gallery cards with the original Table to ensure no data is lost during mapping.
- Visual Audit: Verify backdrop-blur on the Sub-Nav works when scrolling over the Gallery tiles.
</verification>

<success_criteria>
- Gallery page renders extraction results as ProductCards.
- Alternating tile rhythm is visually consistent.
- No standard browser scrollbars or table lines visible in the gallery section.
</success_criteria>

<output>
After completion, create `.planning/phases/01-frontend-apple-design/01-03-SUMMARY.md`
</output>
