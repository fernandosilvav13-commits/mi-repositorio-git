---
phase: 03-wizard-crossref-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/app/wizard/page.tsx
autonomous: false
requirements:
  - WIZ-01

must_haves:
  truths:
    - "User sees an enhanced crossref step in the Wizard with proper sub-step progression"
    - "User can enable/disable cross-reference data matching"
    - "User can upload a new cross-reference file directly in the Wizard"
    - "User can select from previously uploaded cross-reference files"
    - "User sees empty state when crossref is enabled but no files exist"
    - "User sees error states for upload failures, column load failures"
    - "Crossref sub-step state is properly tracked in Wizard navigation"
  artifacts:
    - path: "frontend/src/app/wizard/page.tsx"
      provides: "Enhanced crossref step with upload/selection and proper sub-step flow"
      min_lines: 50
    - path: "frontend/src/app/wizard/page.tsx"
      provides: "Enable/disable toggle, file upload dropzone, file selection list"
  key_links:
    - from: "frontend/src/app/wizard/page.tsx"
      to: "frontend/src/lib/api.ts"
      via: "Import api.crossref for list/get/upload operations"
      pattern: "api.crossref"
---

<objective>
Enhance the Wizard's cross-reference step with proper sub-step flow for enabling, uploading, and selecting cross-reference files.

Purpose: Replace the basic crossref sub-step 0 (yes/no) and sub-step 1 (upload + select) with a polished UX including proper empty state, error states, and smooth progression through the crossref configuration flow.

Output: Enhanced `frontend/src/app/wizard/page.tsx` with improved crossref step UX.
</objective>

<execution_context>
@/home/fernandosilvav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/fernandosilvav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wizard-crossref-integration/03-CONTEXT.md
@.planning/phases/03-wizard-crossref-integration/03-UI-SPEC.md

<interfaces>
From frontend/src/app/wizard/page.tsx:
- Current crossref step structure: subStep 0 (yes/no), subStep 1 (upload + file select)
- State: enableCrossref, crossrefFiles, selectedCrossrefId, crossrefData, uploadingCrossref
- Existing handlers: handleUploadCrossref, handleSelectCrossref
- Sub-step navigation: setSubStep(n), goNext() steps through sub-steps

From frontend/src/lib/api.ts:
```typescript
api.crossref.upload(file: File) => Promise<{id, name, columns, row_count, status}>
api.crossref.list() => Promise<Array<{id, name, file_type, columns, row_count, status}>>
api.crossref.get(id: string) => Promise<{id, name, columns, data, row_count, status}>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enhance crossref step with proper sub-step flow and error handling</name>
  <read_first>
    frontend/src/app/wizard/page.tsx — current wizard page
    frontend/src/components/apple/ConfiguratorCard.tsx — card component
    frontend/src/components/apple/PillChip.tsx — pill chip with variant="selectable"
    frontend/src/lib/api.ts — API helpers
  </read_first>
  <files>frontend/src/app/wizard/page.tsx</files>
  <action>
    Enhance the crossref step section of the Wizard (lines 361-456) by replacing the existing subStep 0, 1, and 2 blocks.

    ### State Additions
    Add these new state variables (near line 45-52 where existing crossref state lives):
    - `crossrefStatus: "idle" | "loading" | "ready" | "mapped"` — default `"idle"`
    - `crossrefError: string | null` — default `null`
    - `crossrefStepError: string | null` — default `null` (for upload/mapping errors inline)

    ### subStep 0 — Yes/No Choice (keep existing but polish)
    Keep existing side-by-side ConfiguratorCard layout with "Sí, cruzar datos" / "No, omitir".
    When user clicks "Sí":
      - Set `enableCrossref = true`, `crossrefStepError = null`  
      - Then set `subStep = 1`
    When user clicks "No":
      - Set `enableCrossref = false`
      - Navigate directly to template step (existing behavior)

    ### subStep 1 — File Upload & Selection (enhance existing)
    Replace the current subStep 1 block (lines 391-435) with an enhanced version:

    #### Error banner (shown when crossrefStepError is set):
    - Show at top of subStep 1, above everything
    - ConfiguratorCard with `className="!border-red-400/50 !bg-red-50/50"`
    - Content: error icon + message text + dismiss button
    - Auto-dismiss: user can click X to clear crossrefStepError
    - Uses copy from UI-SPEC error states

    #### Upload Section (ConfiguratorCard — existing but enhanced):
    Title: "Subir archivo de referencia"
    Subtitle: "CSV, PDF, DOCX o PPT con datos maestros para validación."

    Drop zone area (inside card):
    - Hidden input: `accept=".csv,.pdf,.docx,.ppt,.pptx"` with ref=crossrefFileRef
    - Click zone: dashed border, hover state, Upload icon
    - When uploadingCrossref is true: show spinner/pulse and "Subiendo..." 
    - On file select: call handleUploadCrossref
    - After successful upload: auto-select file, proceed to next step

    Update handleUploadCrossref (around line 161):
    - Add error handling that sets crossrefStepError with message
    - On success: set selectedCrossrefId, load crossrefData columns
    - Navigate to subStep 2 (mapping) after success

    #### Available Files Section (enhanced):
    - Show only when crossrefFiles.length > 0
    - Section label: "Archivos disponibles" (text-[14px] font-semibold text-[#7a7a7a] uppercase tracking-wide)
    - Each file in a ConfiguratorCard:
      - Left: file name (text-[17px] font-semibold) + row_count (text-[14px] text-[#7a7a7a])
      - Right: PillChip with "Usar este" / "Seleccionado"
    - Update handleSelectCrossref (around line 179):
      - Add loading state for column fetching
      - On success: set crossrefData, set crossrefStatus = "ready"
      - On column load error: set crossrefStepError with "Error al cargar columnas" message
      - Navigate to subStep 2 on success

    #### Empty State (when crossrefFiles.length === 0 && !uploading):
    - Show after the upload card (inside the subStep 1 block)
    - ConfiguratorCard with centered content:
      - Database icon (lucide, size={32}, opacity 40%)
      - Text: "Suba un archivo de referencia para comenzar" (text-[17px] font-medium)
      - Subtitle: "Los formatos compatibles son CSV, PDF, DOCX y PPT" (text-[14px] text-[#7a7a7a])
    - This appears when user enabled crossref but hasn't uploaded anything yet

    ### Mapping Navigation
    After file selection or upload succeeds, navigate to subStep 2 (column mapping — to be enhanced in next plan).

    ### Error State Handling
    - Upload "Error al subir archivo. Verifique el formato e intente nuevamente"
    - Column load "Error al cargar columnas del archivo. Intente nuevamente o seleccione otro archivo."
    - Both errors are dismissable via the error banner

    ### Key Behaviors
    - crossrefStatus tracks: "idle" → "loading" (during upload/fetch) → "ready" (file selected, columns loaded) → "mapped" (mapping confirmed)
    - crossrefStepError set on failures, cleared on dismiss or when user starts a new upload
    - All error copy in Spanish per UI-SPEC contract

    ### What NOT to change
    - Do NOT touch the column mapping UI (subStep 2) — that's Plan 03-02
    - Do NOT touch the review step — that's Plan 03-03
    - Do NOT touch template, rules, extract, export, or upload steps
    - Do NOT change the existing handleSelectCrossref flow to subStep 2
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit src/app/wizard/page.tsx 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - TypeScript compilation passes
    - Crossref step shows enable/disable toggle (subStep 0)
    - "Sí, cruzar datos" → transitions to subStep 1
    - "No, omitir" → transitions to template step
    - subStep 1 shows upload dropzone + available files list + empty state
    - File upload shows loading state with "Subiendo..."
    - Upload error shows inline error banner with dismiss
    - Column load error shows inline error banner with dismiss
    - Available files show with file name, row count, and "Usar este"/"Seleccionado" chip
    - Selecting an existing file loads columns and proceeds to subStep 2
    - Empty state renders when crossref enabled but no files uploaded
    - All copy in Spanish per UI-SPEC
  </acceptance_criteria>
  <done>
    The Wizard crossref upload step is enhanced with proper sub-step flow, empty state, error handling, and polished UX.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → DOM | Uploaded file names rendered in Wizard |
| Client → API | File upload and file list requests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | File upload in Wizard | mitigate | Uses existing api.crossref.upload which goes through backend validate_upload. Frontend accept attribute is UX-only. |
| T-03-02 | Spoofing | Error messages | accept | Error messages are hardcoded strings in the frontend, not derived from API response text. Safe from injection. |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` passes
2. Wizard crossref step renders at /wizard with enhanced UX
3. Enable/disable toggle works
4. File upload functions with loading and error states
5. File selection from existing files works
6. Empty state renders when no files
7. Error states render and are dismissable
</verification>

<success_criteria>
1. User can enable crossref and see the upload/select interface
2. User can upload new files or select existing ones
3. Error states are clearly communicated with dismiss option
4. Empty state provides guidance for new users
5. All Wizard crossref step copy is in Spanish
</success_criteria>

<output>
After completion, create `.planning/phases/03-wizard-crossref-integration/03-01-SUMMARY.md`
</output>
