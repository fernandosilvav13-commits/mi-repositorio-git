---
phase: 04
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: [backend/app/api/export.py]
autonomous: true
requirements: [EXP-01, EXP-02]

must_haves:
  truths:
    - "Export API correctly orchestrates consolidation, cross-reference merging, and Excel generation."
    - "The endpoint accepts the new matchKeys payload structure from the Wizard."
  artifacts:
    - path: "backend/app/api/export.py"
      provides: "API endpoint for exporting data with cross-reference"
  key_links:
    - from: "backend/app/api/export.py"
      to: "backend/app/services/crossref_service.py"
      via: "merge_data() call with matchKeys"
    - from: "backend/app/api/export.py"
      to: "backend/app/services/excel_service.py"
      via: "generate() call with crossref_columns"
---

<objective>
Wire the updated CrossrefService and ExcelService into the main export API endpoint. This completes the feature by ensuring the data flow from the Wizard payload results in a correctly formatted and styled Excel export.
</objective>

<execution_context>
@/home/fernandosilvav/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cross-reference-export/04-CONTEXT.md
@.planning/phases/04-cross-reference-export/04-RESEARCH.md
@backend/app/api/export.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate updated services into Export API</name>
  <read_first>backend/app/api/export.py</read_first>
  <files>backend/app/api/export.py</files>
  <action>
    1. Update export_to_excel() to handle the new column_mapping structure: extract matchKeys instead of match_column/crossref_match_column.
    2. Pass matchKeys to crossref_service.merge_data().
    3. Capture the list of crossref columns (output_columns).
    4. Pass output_columns to excel_service.generate() as crossref_columns.
    5. Ensure the final column list passed to generate() includes the new cross-reference columns.
  </action>
  <acceptance_criteria>
    - API accepts matchKeys list in column_mapping.
    - crossref_service.merge_data() is called with correct arguments.
    - excel_service.generate() is called with crossref_columns=output_columns.
    - Final columns list in Excel includes all extraction and cross-ref columns.
  </acceptance_criteria>
  <verify>
    <automated># Integration test with full payload</automated>
  </verify>
  <done>Export API correctly wires the full cross-reference and styling logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API Entry | POST /api/export entry point |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Denial of Service | api/export.py | accept | Processing large files is single-user context; acceptable for current scope. |
</threat_model>

<success_criteria>
- End-to-end export workflow works with cross-reference data.
- Excel file reflects matched data and visual flagging.
</success_criteria>

<output>
After completion, create .planning/phases/04-cross-reference-export/04-03-SUMMARY.md
</output>
