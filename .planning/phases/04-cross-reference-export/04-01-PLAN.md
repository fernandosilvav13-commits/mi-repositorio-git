---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/schemas/crossref.py, backend/app/services/crossref_service.py]
autonomous: true
requirements: [EXP-01]

must_haves:
  truths:
    - "Backend supports matching extraction results with cross-reference data using multiple column pairs (compound keys)."
    - "Lookup performance remains O(N+M) using hashable tuples."
  artifacts:
    - path: "backend/app/schemas/crossref.py"
      provides: "Data contracts for compound match keys"
    - path: "backend/app/services/crossref_service.py"
      provides: "Logic for merging extraction results with reference data"
  key_links:
    - from: "backend/app/schemas/crossref.py"
      to: "backend/app/services/crossref_service.py"
      via: "Type hints and model validation"
---

<objective>
Refactor the cross-reference merging logic to support compound match keys (multiple column pairs) instead of a single column pair. This ensures compatibility with the Wizard flow introduced in Phase 03 and improves matching accuracy.
</objective>

<execution_context>
@/home/fernandosilvav/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cross-reference-export/04-CONTEXT.md
@.planning/phases/04-cross-reference-export/04-RESEARCH.md
@backend/app/schemas/crossref.py
@backend/app/services/crossref_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Crossref Schemas</name>
  <read_first>backend/app/schemas/crossref.py</read_first>
  <files>backend/app/schemas/crossref.py</files>
  <action>
    Add MatchKey model (extractionKey: str, crossrefKey: str) and update ColumnMapping to replace match_column/crossref_match_column with matchKeys: list[MatchKey]. Ensure output_columns remains as list[str].
  </action>
  <acceptance_criteria>
    - backend/app/schemas/crossref.py contains MatchKey class.
    - ColumnMapping class uses matchKeys: list[MatchKey].
    - Schema validates the Wizard payload format: {"matchKeys": [{"extractionKey": "k1", "crossrefKey": "c1"}], "output_columns": ["col1"]}.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from app.schemas.crossref import ColumnMapping; ColumnMapping(matchKeys=[{'extractionKey': 'a', 'crossrefKey': 'b'}], output_columns=['c'])"</automated>
  </verify>
  <done>Schemas updated to support compound keys.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Compound Key Merging</name>
  <read_first>backend/app/services/crossref_service.py</read_first>
  <files>backend/app/services/crossref_service.py</files>
  <action>
    Update merge_data() signature to accept match_keys: list[dict] (from schema). Build lookup dictionary using tuples of (key1, key2, ...) as keys. Normalize keys using str().strip().lower() for robust matching. Fallback to "NO ENCONTRADO" for missing matches.
  </action>
  <acceptance_criteria>
    - merge_data() accepts match_keys instead of single column strings.
    - Lookup uses tuples for O(1) matching with multiple keys.
    - If match_keys is [{"extractionKey": "RUT", "crossrefKey": "RUT_REF"}], it matches successfully.
    - Unmatched rows have "NO ENCONTRADO" in output columns.
  </acceptance_criteria>
  <verify>
    <automated># Manual verification via script or wait for API integration in Plan 03</automated>
  </verify>
  <done>Merge logic supports multiple match keys with O(1) performance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Schema Validation | Incoming JSON from Wizard is validated against ColumnMapping schema |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | CrossrefService.merge_data | mitigate | Normalize keys (strip/lower) to prevent trivial bypass via case/whitespace |
</threat_model>

<success_criteria>
- Backend can process extraction results and merge with reference data using multiple keys.
- Data structures are consistent with Phase 03 Wizard output.
</success_criteria>

<output>
After completion, create .planning/phases/04-cross-reference-export/04-01-SUMMARY.md
</output>
