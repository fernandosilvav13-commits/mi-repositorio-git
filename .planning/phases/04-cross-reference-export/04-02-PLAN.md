---
phase: 04
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [backend/app/services/excel_service.py]
autonomous: true
requirements: [EXP-02]

must_haves:
  truths:
    - "Cross-reference columns in the exported Excel are clearly distinguishable (prefixed with [REF])."
    - "Rows that failed to match cross-reference data are highlighted in yellow (amber)."
  artifacts:
    - path: "backend/app/services/excel_service.py"
      provides: "Excel generation with visual status indicators"
  key_links:
    - from: "backend/app/services/excel_service.py"
      to: "openpyxl.styles.PatternFill"
      via: "Row highlighting logic"
---

<objective>
Enhance the Excel generation service to provide visual feedback to the user. This includes identifying columns that originated from cross-reference data and highlighting rows that did not find a match, aiding in data verification.
</objective>

<execution_context>
@/home/fernandosilvav/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cross-reference-export/04-CONTEXT.md
@.planning/phases/04-cross-reference-export/04-RESEARCH.md
@backend/app/services/excel_service.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Excel Visual Styling</name>
  <read_first>backend/app/services/excel_service.py</read_first>
  <files>backend/app/services/excel_service.py</files>
  <action>
    1. Update generate() signature to accept crossref_columns: list[str] | None = None.
    2. In header generation, prefix columns in crossref_columns with "[REF] ".
    3. In row generation, detect unmatched rows: if crossref_columns is not empty and ALL cells in those columns are "NO ENCONTRADO", set is_unmatched = True.
    4. Apply YELLOW_FILL (FFFF44) to the entire row if is_unmatched is True.
  </action>
  <acceptance_criteria>
    - generate() accepts crossref_columns parameter.
    - Headers for columns in crossref_columns are prefixed with "[REF] ".
    - Rows where all cross-ref data is "NO ENCONTRADO" have YELLOW_FILL background across all cells.
    - Critical failure (all columns NO ENCONTRADO) still uses RED_FILL.
  </acceptance_criteria>
  <verify>
    <automated># Manual check of generated file or mock test for style application</automated>
  </verify>
  <done>ExcelService applies visual flagging and header prefixes correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File I/O | Excel file is written to local disk in output_dir |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | ExcelService.generate | accept | Visual flagging reveals match status, which is the intended feature. |
</threat_model>

<success_criteria>
- Exported Excel files provide clear visual indication of matched vs. unmatched cross-reference data.
- Headers are correctly prefixed to distinguish source data.
</success_criteria>

<output>
After completion, create .planning/phases/04-cross-reference-export/04-02-SUMMARY.md
</output>
