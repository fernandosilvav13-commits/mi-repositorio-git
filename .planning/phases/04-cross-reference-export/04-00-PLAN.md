---
phase: 04
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [backend/requirements.txt, backend/tests/conftest.py, backend/tests/test_crossref_merge.py, backend/tests/test_excel_styling.py, backend/tests/test_export_api.py]
autonomous: true
requirements: [EXP-01, EXP-02]
---

<objective>
Establish the testing infrastructure for Phase 04. This includes installing dependencies and creating failing (RED) tests for compound key merging, Excel visual styling, and Export API orchestration. These tests will drive the implementation in subsequent plans.
</objective>

<execution_context>
@/home/fernandosilvav/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-cross-reference-export/04-CONTEXT.md
@.planning/phases/04-cross-reference-export/04-RESEARCH.md
@.planning/phases/04-cross-reference-export/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup test environment and dependencies</name>
  <read_first>backend/requirements.txt</read_first>
  <files>backend/requirements.txt, backend/tests/conftest.py</files>
  <action>
    1. Add pytest and pytest-asyncio to backend/requirements.txt.
    2. Create backend/tests/ directory.
    3. Create backend/tests/conftest.py to define common fixtures (mock database, mock crossref files).
  </action>
  <acceptance_criteria>
    - pytest and pytest-asyncio are present in requirements.txt.
    - backend/tests/ directory exists.
    - pytest --version runs successfully in the backend environment.
  </acceptance_criteria>
  <verify>
    <automated>pip install -r backend/requirements.txt && pytest --version</automated>
  </verify>
  <done>Testing infrastructure is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Create failing unit tests for merge logic and styling</name>
  <read_first>backend/app/services/crossref_service.py</read_first>
  <files>backend/tests/test_crossref_merge.py, backend/tests/test_excel_styling.py</files>
  <action>
    1. Create backend/tests/test_crossref_merge.py with tests for compound key matching (multiple columns) and normalization (strip/lower), per analogs in 04-PATTERNS.md.
    2. Create backend/tests/test_excel_styling.py with tests for [REF] prefixing and YELLOW_FILL (FFFF44) application for unmatched rows.
  </action>
  <acceptance_criteria>
    - Test files exist in backend/tests/.
    - Running pytest backend/tests/test_crossref_merge.py fails (as logic is not yet implemented).
    - Running pytest backend/tests/test_excel_styling.py fails.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_crossref_merge.py backend/tests/test_excel_styling.py</automated>
  </verify>
  <done>Unit tests created and failing as expected.</done>
</task>

<task type="auto">
  <name>Task 3: Create failing integration test for Export API</name>
  <read_first>backend/app/api/export.py</read_first>
  <files>backend/tests/test_export_api.py</files>
  <action>
    Create backend/tests/test_export_api.py to test the POST /api/export endpoint with the new matchKeys payload structure. Use TestClient from fastapi.testclient.
  </action>
  <acceptance_criteria>
    - backend/tests/test_export_api.py exists.
    - Running pytest backend/tests/test_export_api.py fails due to schema/logic mismatch.
  </acceptance_criteria>
  <verify>
    <automated>pytest backend/tests/test_export_api.py</automated>
  </verify>
  <done>Integration test created and failing as expected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Internal Tests | Test code runs in development environment only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00 | Information Disclosure | backend/tests/ | mitigate | Ensure test files do not contain real production secrets or PII. |
/threat_model>

<success_criteria>
- Testing framework is installed and configured.
- Failing tests are established for all phase requirements (EXP-01, EXP-02).
</success_criteria>

<output>
After completion, create .planning/phases/04-cross-reference-export/04-00-SUMMARY.md
</output>
