---
phase: 02-crossref-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migration.sql
  - backend/app/api/crossref.py
autonomous: true
requirements:
  - CRSS-01
  - CRSS-02

must_haves:
  truths:
    - "The crossref_files table stores match status per file"
    - "API endpoints return status in every response about a crossref file"
  artifacts:
    - path: "supabase/migration.sql"
      provides: "Status column on crossref_files with default 'unmatched'"
      contains: "add column if not exists status text not null default 'unmatched'"
    - path: "backend/app/api/crossref.py"
      provides: "Status field in upload response, list response, and get response"
      exports: ["status in upload response JSON", "status in GET /files array", "status in GET /files/{id}"]
  key_links:
    - from: "backend/app/api/crossref.py POST /upload"
      to: "supabase/migration.sql crossref_files.status column"
      via: "INSERT includes status field"
      pattern: '"status": "unmatched"'
    - from: "backend/app/api/crossref.py GET /files"
      to: "supabase/migration.sql crossref_files.status column"
      via: "SELECT includes status column"
      pattern: '.select(".*status.*")'
---

<objective>
Add match status tracking to the cross-reference data backend.

Purpose: The frontend needs to display match status per file (matched/unmatched/processing). Currently the crossref_files table has no status column and the API doesn't return one. This plan adds the column to the schema and updates all API responses to include it.

Output:
- supabase/migration.sql with ALTER TABLE adding status column + index
- backend/app/api/crossref.py with status in all response shapes
- Database migration applied (via supabase CLI or manual)
</objective>

<execution_context>
@/home/fernandosilvav/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/fernandosilvav/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-crossref-page-redesign/02-CONTEXT.md
@.planning/phases/02-crossref-page-redesign/02-RESEARCH.md
@.planning/phases/02-crossref-page-redesign/02-PATTERNS.md

<interfaces>
<!-- Key types and contracts extracted from source files for executor use -->
From supabase/migration.sql - existing crossref_files table (lines 100-110):
```sql
create table if not exists crossref_files (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  file_type text not null,
  columns jsonb not null default '[]',
  data jsonb not null default '[]',
  row_count int default 0,
  created_by uuid references auth.users(id),
  created_at timestamptz default now()
);
```

From backend/app/api/crossref.py - current endpoint signatures:
- POST /upload: Insert without "status", returns {id, name, columns, row_count}
- GET /files: .select("id,name,file_type,columns,row_count,created_at"), returns array
- GET /files/{id}: .select("*"), returns full row
- DELETE /files/{id}: deletes and returns {ok: true}

From backend/app/schemas/crossref.py:
```python
class ColumnMapping(BaseModel):
    match_column: str
    crossref_match_column: str
    output_columns: list[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add status column to crossref_files table schema</name>
  <read_first>
    supabase/migration.sql — current schema to add migration to end of file (after line 130)
  </read_first>
  <files>supabase/migration.sql</files>
  <action>
    Append two new SQL statements AFTER the last line of migration.sql (after the existing index block ending at line 130). Do not modify any existing SQL.

    1. ALTER TABLE migration: `alter table crossref_files add column if not exists status text not null default 'unmatched';`
    2. INDEX creation: `create index if not exists idx_crossref_files_status on crossref_files(status);`

    Follow existing SQL conventions in the file:
    - Prefix with a Spanish comment header `-- ...` describing the change (e.g. "-- Columna de estado para tracking de matching")
    - Use `if not exists` guard on both ALTER and CREATE INDEX statements
    - Indentation: no leading indentation for SQL keywords (same as existing patterns)
    - Place after the last index (line 130) separated by one blank line
    - This is additive only — never change any existing statements or their order

    Then, attempt to push the migration:
    - Run: `npx supabase db push` (if supabase CLI is available and linked)
    - If supabase CLI is NOT available or not configured, print a clear message: "`supabase db push` could not be run automatically. Apply the new ALTER TABLE and CREATE INDEX statements manually via Supabase Dashboard SQL Editor."
  </action>
  <verify>
    <automated>grep -q "add column if not exists status text not null default 'unmatched'" supabase/migration.sql && grep -q "create index if not exists idx_crossref_files_status on crossref_files(status)" supabase/migration.sql</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migration.sql` contains: `alter table crossref_files add column if not exists status text not null default 'unmatched';` after line 130
    - `supabase/migration.sql` contains: `create index if not exists idx_crossref_files_status on crossref_files(status);` after the ALTER TABLE
    - The migration SQL has been attempted via `npx supabase db push` or the user has been instructed to apply it manually
    - No existing SQL in migration.sql has been modified
  </acceptance_criteria>
  <done>
    Migration SQL is appended to supabase/migration.sql. Status column exists on crossref_files table in the database.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Return status in crossref API responses</name>
  <read_first>
    backend/app/api/crossref.py — current endpoint implementations to modify
  </read_first>
  <files>backend/app/api/crossref.py</files>
  <action>
    Modify backend/app/api/crossref.py to include status in all crossref API responses. Make exactly these three changes:

    1. POST /upload handler (the `upload_crossref` function): 
       - In the `.insert({...})` call, add `"status": "unmatched"` to the dict
       - In the return dict, add `"status": "unmatched"` key-value pair
       - The return dict should become: `{"id": ..., "name": ..., "columns": ..., "row_count": ..., "status": "unmatched"}`
       
    2. GET /files handler (the `list_files` function):
       - Change the `.select(...)` string to include `status`:
         Change: `.select("id,name,file_type,columns,row_count,created_at")`
         To: `.select("id,name,file_type,columns,row_count,created_at,status")`

    3. GET /files/{id} handler (the `get_file` function):
       - Already uses `.select("*")` so no change needed — it will automatically include the new status column.
       - Do NOT modify this function.

    Do NOT modify the DELETE endpoint, the router definition, imports, or any other part of the file.
    Keep all existing code unchanged except the two targeted changes above.
  </action>
  <verify>
    <automated>grep -q '"status": "unmatched"' backend/app/api/crossref.py && grep -q "status" backend/app/api/crossref.py | grep "select"</automated>
  </verify>
  <acceptance_criteria>
    - POST /upload response includes `"status": "unmatched"` in both the insert dict and the return dict
    - GET /files SELECT string includes `status` in the field list: `.select("id,name,file_type,columns,row_count,created_at,status")`
    - GET /files/{id} is unchanged (already uses `.select("*")`)
    - No other lines in crossref.py are modified
  </acceptance_criteria>
  <done>
    All crossref API endpoints return `status` field. Upload defaults to "unmatched". List and get endpoints include the database value.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | HTTP request carries crossref file data and metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | POST /upload — status field in insert | mitigate | status is hardcoded to "unmatched" on insert, not user-controllable. Supabase JS SDK parameterizes the insert, preventing SQL injection. |
| T-02-02 | Information Disclosure | GET /files — status field added to SELECT | accept | Status values are non-sensitive (matched/unmatched/processing). Existing auth dependency on the router already scopes results to created_by. |
| T-02-03 | Spoofing | Migration SQL execution | mitigate | Schema change is additive (ALTER TABLE ADD COLUMN IF NOT EXISTS). No existing data is modified. If applied manually, user must have Supabase dashboard access. |
</threat_model>

<verification>
1. `grep` confirms status column and index in migration.sql
2. `grep` confirms status in API response fields
3. `grep` confirms no unintended changes to other parts of crossref.py
4. Migration is applied to the database (verified via supabase CLI or manual confirmation)
</verification>

<success_criteria>
1. `crossref_files` table has `status` column with default `'unmatched'`
2. All crossref API endpoints return `status` in their response
3. Migration is applied to the live database
4. No existing functionality is broken (additive change only)
</success_criteria>

<output>
After completion, create `.planning/phases/02-crossref-page-redesign/02-01-SUMMARY.md`
</output>
