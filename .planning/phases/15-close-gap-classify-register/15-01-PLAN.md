# Plan 15-01: Register classify router + preprocess input

**Phase:** 15 - Close Gap: Register /api/classify in main.py
**Plan:** 01
**Wave:** 1
**Objective:** Register the classify router in main.py and add preprocessing to the classify endpoint so standalone classification matches pipeline behavior.
**Gap closure:** true

## Tasks

### Task 1: Register classify router in main.py

File: `backend/app/main.py`

- Add `from app.api import classify` to the existing import line (after `crossref`)
- Add `app.include_router(classify.router, prefix="/api/classify", tags=["Clasificación"])` after the existing router registrations, before the health endpoint

### Task 2: Add preprocessing to classify endpoint

File: `backend/app/api/classify.py`

- Add `from app.services.preprocessor import preprocessing_pipeline` import
- Before classification, call `preprocessed = preprocessing_pipeline.process(req.text)` and use `preprocessed.cleaned_text` instead of `req.text` for the classification call
- This ensures the standalone endpoint produces identical results to what the two-pass pipeline produces internally

## Verification

- POST /api/classify returns 200 instead of 404
- Classification runs on preprocessed text
- All existing tests continue to pass
