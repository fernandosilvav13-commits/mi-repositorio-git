# Plan 15-02: Activate two-pass pipeline + fix prompt path

**Phase:** 15 - Close Gap: Register /api/classify in main.py
**Plan:** 02
**Wave:** 1
**Objective:** Flip CVProcessor use_two_pass default to True and fix PromptResolver path in extraction_pipeline.py to use absolute path pattern matching section_detector.py.
**Gap closure:** true

## Tasks

### Task 1: Flip CVProcessor two-pass default

File: `backend/app/services/cv_processor.py`

- Change `def __init__(self, matricula_csv: str = "mineduc_matricula.csv", use_two_pass: bool = False):` to `def __init__(self, matricula_csv: str = "mineduc_matricula.csv", use_two_pass: bool = True):`
- Keep `use_two_pass=False` as an explicit opt-out parameter for backward compatibility

### Task 2: Fix PromptResolver path in extraction_pipeline.py

File: `backend/app/services/extraction_pipeline.py`

- Change `prompt_resolver = PromptResolver()` to pass the absolute path pattern matching section_detector.py:
  ```python
  from pathlib import Path
  prompt_resolver = PromptResolver(Path(__file__).resolve().parent.parent.parent / "prompts")
  ```
- Add `from pathlib import Path` import if not already present

## Verification

- CVProcessor() defaults to use_two_pass=True
- use_two_pass=False still works as explicit opt-out
- PromptResolver resolves prompts correctly from any working directory
- All existing tests continue to pass
